#get essentials

start script

table {
    strings {
        createTable("data")
    }
}
data = 
    title
    stored
    app
proceed 1
inside(data)
    title = ("title")
    stored = ("directory")
    app = ("app")
end script